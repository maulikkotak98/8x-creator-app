/**
 * Shared spacing and border-radius scale.
 * Keeps visual rhythm consistent across screens.
 */
export const Spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 28,
  screenTop: 56,
  screenBottom: 100,
} as const;

export const Radius = {
  sm: 8,
  md: 12,
  lg: 14,
  xl: 16,
  full: 999,
} as const;
